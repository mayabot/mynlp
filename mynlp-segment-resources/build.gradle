version = "1.1.0"

def publish_resource = false

if(!publish_resource){
    project.afterEvaluate {
        project.tasks.withType(AbstractPublishToMaven.class){
            enabled = false
        }
    }
}
