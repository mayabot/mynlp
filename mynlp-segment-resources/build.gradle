version = "1.1.0"

description = "segment使用的基本词典资源。独立版本发布"
def publish_resource = false

if(!publish_resource){
    project.afterEvaluate {
        project.tasks.withType(AbstractPublishToMaven.class){
            enabled = false
        }
    }
}

sourceJar{
    getMainSpec().exclude("maya_data/**")
}